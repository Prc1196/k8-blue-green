kind: Service
apiVersion: v1
metadata:
  name: backend
  namespace: expense
spec:
  selector:   #lables are used as selectors
    app: backend
    tier: backend
    purpose: expense
  ports:
  - protocol: TCP
    port: 80  #service port
    targetPort: 80
   